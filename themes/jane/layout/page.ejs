<div class="main">
	<div class="post">
		<h3 class="date" title="<%= date(page.date, config.time_format)%>"><%= date(page.date, config.date_format) %></h3>
		<h1><%= page.title %></h1>
		<div class="content">
			<%- page.content %>
		</div>
	</div>
	<% if (theme.page_comment) { %>
		<%- partial('_partial/comments',{}) %>
	<% } %>
</div>

